// TODO**

// Given a db model, this function returns a function which allows find a model by record id, then updates and saves this record.
// params are the model you are trying to save the record to.
// ex.
// const updateUser = updateOne(User)
// const updatedUser = await updateUser({...})
