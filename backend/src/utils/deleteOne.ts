// TODO**

// Given a db model, this function returns a function which allows find a model by record id, then deletes this record from the model
// params are the model you are trying to save the record to.
// ex.
// const deleteUser = deleteOne(User)
// const deletedUser = await deleteUser({...}) 
